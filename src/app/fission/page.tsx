import FissionWorldCanvas from './FissionWorldCanvas';

export default function Sandbox() {
  return (
    <main style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', marginTop: '0rem' }}>
      <FissionWorldCanvas />
    </main>
  );
}
